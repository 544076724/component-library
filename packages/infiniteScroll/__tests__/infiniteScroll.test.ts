import { mount } from '@vue/test-utils'
import Element from '../src/infiniteScroll.vue'

describe('c-dhn-infiniteScroll', () => {
    test('infiniteScroll-text',() => {
        // const wrapper = mount(Element)
        // expect(wrapper.html()).toContain('div')
    })
})
