import { mount } from '@vue/test-utils'
import Element from '../src/asyncImg.vue'

describe('c-dhn-asyncImg', () => {
    test('asyncImg-text',() => {
        // const wrapper = mount(Element)
        // expect(wrapper.html()).toContain('div')
    })
})
