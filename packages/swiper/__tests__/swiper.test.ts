import { mount } from '@vue/test-utils'
import Element from '../src/swiper.vue'

describe('c-dhn-swiper', () => {
    test('swiper-text',() => {
        // const wrapper = mount(Element)
        // expect(wrapper.html()).toContain('span')
    })
})
